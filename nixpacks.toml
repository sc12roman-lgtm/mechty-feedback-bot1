[variables]
    NIXPACKS_PYTHON_VERSION = "3.10"

[phases.setup]
    nixPkgs = ["python310", "gcc"]

[phases.install]
    cmds = ["python -m venv /opt/venv && /opt/venv/bin/pip install --upgrade pip && /opt/venv/bin/pip install aiogram==2.25.1"]

[start]
    cmd = "/opt/venv/bin/python bot.py"
